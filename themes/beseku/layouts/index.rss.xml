{{ printf "<?xml version=\"1.0\" encoding=\"utf-8\" standalone=\"yes\"?>" | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ .Site.Title }}</title>
    <link>{{ .Permalink }}</link>
    <description>{{ .Summary | default "¿?" }}</description>
    <generator>Hugo</generator>

    {{ with .Site.LanguageCode }}
    <language>{{.}}</language>
    {{end}}

    {{ with .Site.Params.Author.name }}
    <managingEditor>{{.}}</managingEditor>
    {{end}}

    {{ with .Site.Params.Author.name }}
    <webMaster>{{.}}</webMaster>
    {{end}}

    {{ with .Site.Copyright }}
    <copyright>{{.}}</copyright>
    {{end}}

    {{ if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>
    {{ end }}

    {{ with .OutputFormats.Get "RSS" }}
    <link href="{{.Permalink}}" rel="self" title="{{ $.Site.Title }}" type="application/rss+xml" />
    {{ end }}

    {{ with (where .Site.RegularPages "Parent.Type" "in" (slice "blog")) }}
    {{ range . }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>

      {{ with .Site.Params.Author.email }}
      <author>{{.}}{{ with $.Site.Params.Author.name }} ({{.}}){{end}}</author>
      {{end}}

      <guid>{{ .Permalink }}</guid>
      <description>{{ html .Content }}</description>
    </item>
    {{ end }}
    {{ end }}
  </channel>
</rss>